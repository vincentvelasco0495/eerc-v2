import { TmIconButton } from './tm-icon-button';

/** Semantic alias for icon actions (rounded, hover, active from theme). */
export function ToolbarButton(props) {
  return <TmIconButton {...props} />;
}
