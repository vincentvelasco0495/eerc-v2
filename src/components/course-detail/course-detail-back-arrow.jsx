/**
 * Chevron back control used on the styled course shell (`CourseHeader`) and aligned surfaces
 * (e.g. full-page text lesson) so navigation reads as one system.
 */
export function CourseDetailBackArrowSvg() {
  return (
    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden>
      <path
        d="M15 18l-6-6 6-6"
        stroke="currentColor"
        strokeWidth="2"
        strokeLinecap="round"
        strokeLinejoin="round"
      />
    </svg>
  );
}
